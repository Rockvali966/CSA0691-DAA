#include <stdio.h>
int main()
{
	int i,n,a[100];
	printf("enter the size :");
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
	    printf("emter the number %d=",i+1);
		scanf("%d",&a[i]);
	}
	for(i=0;i<n;i++)
	{
		if(a[0]<a[i])
		{
			a[0]=a[i];
		}
	}
	printf("the largest number is %d",a[0]);
	return 0;
}
